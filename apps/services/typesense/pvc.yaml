apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: typesense-data
  annotations:
    csi.hetzner.cloud/volume-name: "typesense-data"
spec:
  storageClassName: hcloud-volumes
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
