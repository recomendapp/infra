resources:
  - secret.yaml
  - service.yaml
  - statefulset.yaml
  - setup-job.yaml
  - backup-secret.yaml
  - backup-cronjob.yaml
  # No ingress here: at cutover, minio/ingress.yaml's backend service is
  # switched to `rustfs`, keeping the existing medias.recomend.app TLS cert.
  # migrate-job.yaml is intentionally not listed (manual one-off).
