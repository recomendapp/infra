apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rustfs
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  tls:
    - hosts:
        - medias.recomend.app
      # Inherited from the MinIO ingress: the cert-manager Secret outlives the
      # old Ingress, so reusing its name avoids a reissue (and a TLS gap).
      secretName: minio-tls
  rules:
    - host: medias.recomend.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rustfs
                port:
                  number: 9000
