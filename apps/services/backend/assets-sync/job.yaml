apiVersion: batch/v1
kind: Job
metadata:
  name: assets-sync
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  template:
    spec:
      imagePullSecrets:
        - name: github-credentials
      containers:
        - name: assets-sync
          image: ghcr.io/recomendapp/assets:b68-522e0ea
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: minio-secret
      restartPolicy: Never
  backoffLimit: 4
