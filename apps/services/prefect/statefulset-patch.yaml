# This is a Kustomize strategic merge patch to add the Velero backup annotation
# to the PostgreSQL StatefulSet generated by the Prefect Helm chart.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: prefect-postgresql
spec:
  template:
    metadata:
      annotations:
        backup.velero.io/backup-volumes: data
