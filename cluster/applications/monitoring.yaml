apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monitoring
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "50"
spec:
  project: default
  sources:
    - repoURL: 'https://prometheus-community.github.io/helm-charts'
      chart: kube-prometheus-stack
      targetRevision: 79.7.1
      helm:
        valuesObject:
          grafana:
            enabled: true
            ingress:
              enabled: true
              ingressClassName: traefik
              annotations:
                cert-manager.io/cluster-issuer: letsencrypt-production
                traefik.ingress.kubernetes.io/router.entrypoints: websecure
              hosts:
                - grafana.recomend.app
              tls:
                - secretName: grafana-tls
                  hosts:
                    - grafana.recomend.app
            sidecar:
              dashboards:
                enabled: true
                label: grafana_dashboard
                labelValue: "1"
                searchNamespace: ALL
            resources:
              requests:
                cpu: 100m
                memory: 128Mi
              limits:
                cpu: 500m
                memory: 512Mi

          prometheus-node-exporter:
            hostPort: 19100
            resources:
              requests:
                cpu: 50m
                memory: 64Mi
              limits:
                cpu: 100m
                memory: 128Mi

          prometheus:
            prometheusSpec:
              resources:
                requests:
                  cpu: 200m
                  memory: 1Gi
                limits:
                  cpu: "1"
                  memory: 4Gi
              storageSpec:
                volumeClaimTemplate:
                  spec:
                    storageClassName: hcloud-volumes
                    accessModes: ["ReadWriteOnce"]
                    resources:
                      requests:
                        storage: 20Gi
          alertmanager:
            alertmanagerSpec:
              secrets:
                - monitoring-secrets
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 512Mi
            config:
              global:
                resolve_timeout: 5m
              route:
                group_by: ['namespace', 'alertname']
                group_wait: 30s
                group_interval: 5m
                repeat_interval: 12h
                receiver: discord-webhook
                routes:
                  - receiver: 'null'
                    matchers:
                      - alertname = "Watchdog"
              receivers:
                - name: 'null'
                - name: discord-webhook
                  discord_configs:
                    - webhook_url_file: /etc/alertmanager/secrets/monitoring-secrets/ALERTMANAGER_DISCORD_WEBHOOK_URL
          kube-state-metrics:
            resources:
              requests:
                cpu: 100m
                memory: 128Mi
              limits:
                cpu: 200m
                memory: 256Mi

    - repoURL: https://github.com/recomendapp/infra.git
      targetRevision: main
      path: cluster/services/monitoring
      directory:
        recurse: true

  destination:
    server: "https://kubernetes.default.svc"
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true